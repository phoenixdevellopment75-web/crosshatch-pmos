name: Build postmarketOS for Pixel 3 XL

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      ui:
        description: 'User Interface'
        required: true
        default: 'plasma-mobile'
        type: choice
        options:
          - plasma-mobile
          - phosh
          - sxmo-de-sway
          - console
      user_password:
        description: 'User password for SSH login'
        required: true
        default: 'pixel3xl'

jobs:
  build:
    name: Build postmarketOS Image
    runs-on: ubuntu-latest
    timeout-minutes: 180

    steps:
      # ── 1. Install system dependencies ───────────────────────────────
      - name: Install system dependencies
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y \
            python3 python3-pip python3-venv python3-setuptools \
            git openssl binfmt-support qemu-user-static \
            coreutils procps sudo e2fsprogs

      # ── 2. Install pmbootstrap ───────────────────────────────────────
      - name: Install pmbootstrap
        run: |
          set -euo pipefail
          pip3 install --user git+https://gitlab.postmarketos.org/postmarketOS/pmbootstrap.git
          echo "$HOME/.local/bin" >> $GITHUB_PATH
          export PATH="$HOME/.local/bin:$PATH"
          pmbootstrap --version
          echo "✅ pmbootstrap installed"

      # ── 3. Configure pmbootstrap ─────────────────────────────────────
      - name: Configure pmbootstrap
        run: |
          set -euo pipefail
          export PATH="$HOME/.local/bin:$PATH"

          CONFIG_DIR="$HOME/.config"
          mkdir -p "$CONFIG_DIR"

          cat > "$CONFIG_DIR/pmbootstrap_v3.cfg" << 'EOF'
          [pmbootstrap]
          aports = /home/runner/.local/var/pmbootstrap/cache_git/pmaports
          build_default_device_arch = True
          boot_size = 512
          channel = systemd-edge
          device = google-crosshatch
          extra_space = 2048
          hostname = pixel3xl
          is_default_channel = False
          jobs = 2
          kernel = google-crosshatch-mainline
          keymap =
          locale = en_US.UTF-8
          mirror_alpine = http://dl-cdn.alpinelinux.org/alpine/
          nonfree_firmware = True
          nonfree_userland = True
          ssh_keys = True
          ssh_key_glob = ~/.ssh/id_*.pub
          timezone = Asia/Kolkata
          ui = ${{ github.event.inputs.ui || 'plasma-mobile' }}
          ui_extras = True
          user = user
          work = /home/runner/.local/var/pmbootstrap
          EOF

          sed -i 's/^          //' "$CONFIG_DIR/pmbootstrap_v3.cfg"
          echo "✅ pmbootstrap configured"

      # ── 4. Pre-initialize workdir ───────────────────────────────────
      - name: Pre-initialize workdir
        run: |
          set -euo pipefail
          WORK_DIR="/home/runner/.local/var/pmbootstrap"
          mkdir -p "$WORK_DIR/cache_git"
          echo "8" > "$WORK_DIR/version"

      # ── 5. Inject persistent signing key ─────────────────────────────
      #
      #  WHY: pmbootstrap generates a random RSA key pair each fresh run to
      #  sign locally built packages. Cached .apk files from a previous run
      #  are signed with THAT old key. On a new runner the key is different
      #  → apk refuses the packages with exit code 78 (UNTRUSTED_SIGNATURE).
      #
      #  FIX: We store a FIXED key pair as GitHub Secrets (PMOS_SIGNING_KEY
      #  and PMOS_SIGNING_KEY_PUB), inject them into the exact paths that
      #  pmbootstrap / abuild-keygen would create, and mark them so pmbootstrap
      #  skips key generation entirely (config_abuild/abuild.conf exists).
      #
      - name: Inject persistent signing key
        env:
          PMOS_SIGNING_KEY: ${{ secrets.PMOS_SIGNING_KEY }}
          PMOS_SIGNING_KEY_PUB: ${{ secrets.PMOS_SIGNING_KEY_PUB }}
        run: |
          set -euo pipefail
          WORK_DIR="/home/runner/.local/var/pmbootstrap"
          KEY_NAME="pmos@local-62f4da00.rsa"

          # Create abuild config dir (pmbootstrap mounts this as mnt/pmbootstrap/abuild-config)
          mkdir -p "$WORK_DIR/config_abuild"

          # Write private key
          echo "$PMOS_SIGNING_KEY" | base64 -d > "$WORK_DIR/config_abuild/${KEY_NAME}"
          chmod 600 "$WORK_DIR/config_abuild/${KEY_NAME}"

          # Write public key
          echo "$PMOS_SIGNING_KEY_PUB" > "$WORK_DIR/config_abuild/${KEY_NAME}.pub"
          chmod 644 "$WORK_DIR/config_abuild/${KEY_NAME}.pub"

          # Write abuild.conf so pmbootstrap sees keys as already generated
          cat > "$WORK_DIR/config_abuild/abuild.conf" << ABUILD
          PACKAGER_PRIVKEY="${WORK_DIR}/config_abuild/${KEY_NAME}"
          ABUILD

          # Also place the public key in config_apk_keys (trusted keys dir for apk)
          mkdir -p "$WORK_DIR/config_apk_keys"
          cp "$WORK_DIR/config_abuild/${KEY_NAME}.pub" "$WORK_DIR/config_apk_keys/"

          echo "✅ Signing keys injected:"
          ls -la "$WORK_DIR/config_abuild/"
          ls -la "$WORK_DIR/config_apk_keys/"

      # ── 6. Restore pmbootstrap packages cache ────────────────────────
      - name: Restore pmbootstrap packages cache
        uses: actions/cache/restore@v4
        with:
          path: |
            /home/runner/.local/var/pmbootstrap/packages
            /home/runner/.local/var/pmbootstrap/cache_apk
          key: pmbootstrap-packages-${{ runner.os }}-stable
          restore-keys: |
            pmbootstrap-packages-${{ runner.os }}-

      # ── 7. Migrate cached packages to systemd-edge ───────────────────
      - name: Migrate cached packages to systemd-edge
        run: |
          set -euo pipefail
          WORK_DIR="/home/runner/.local/var/pmbootstrap"

          if [ -d "$WORK_DIR/packages/edge" ]; then
            echo "📋 Migrating cached packages from edge → systemd-edge..."
            mkdir -p "$WORK_DIR/packages/systemd-edge"
            cp -rn "$WORK_DIR/packages/edge/"* "$WORK_DIR/packages/systemd-edge/" 2>/dev/null || true
            echo "✅ Migration done"
          else
            echo "ℹ️ No packages/edge cache found — will build from scratch"
          fi

          echo ""
          echo "📦 Available local packages:"
          find "$WORK_DIR/packages" -name "*.apk" 2>/dev/null | head -20 || echo "(none)"

      # ── 8. Clone pmaports ────────────────────────────────────────────
      - name: Clone pmaports
        run: |
          set -euo pipefail
          export PATH="$HOME/.local/bin:$PATH"
          PMAPORTS_DIR="/home/runner/.local/var/pmbootstrap/cache_git/pmaports"

          git clone --depth=1 \
            https://gitlab.postmarketos.org/postmarketOS/pmaports.git \
            "$PMAPORTS_DIR"
          echo "✅ pmaports cloned"

      # ── 9. Verify configuration ──────────────────────────────────────
      - name: Verify configuration status
        run: |
          set -euo pipefail
          export PATH="$HOME/.local/bin:$PATH"
          pmbootstrap status
          echo "  Device: $(pmbootstrap config device)"
          echo "  UI:     $(pmbootstrap config ui)"

      # ── 10. Build kernel ─────────────────────────────────────────────
      - name: Build kernel (linux-google-crosshatch-mainline)
        run: |
          set -euo pipefail
          export PATH="$HOME/.local/bin:$PATH"
          echo "🔨 Building kernel (uses cache if available)..."
          pmbootstrap build linux-google-crosshatch-mainline 2>&1
          echo "✅ Kernel ready"

      # ── 11. Build device package ─────────────────────────────────────
      - name: Build device package
        run: |
          set -euo pipefail
          export PATH="$HOME/.local/bin:$PATH"
          echo "📦 Building device package (uses cache if available)..."
          pmbootstrap build device-google-crosshatch 2>&1
          echo "✅ Device package ready"

      # ── 12. Save pmbootstrap packages cache ──────────────────────────
      - name: Save pmbootstrap packages cache
        uses: actions/cache/save@v4
        if: always()
        with:
          path: |
            /home/runner/.local/var/pmbootstrap/packages
            /home/runner/.local/var/pmbootstrap/cache_apk
          key: pmbootstrap-packages-${{ runner.os }}-stable

      # ── 13. Create installation image ────────────────────────────────
      - name: Create installation image
        run: |
          set -euo pipefail
          export PATH="$HOME/.local/bin:$PATH"

          echo "💾 Running pmbootstrap install..."

          printf '%s\n%s\n' \
            "${{ github.event.inputs.user_password || 'pixel3xl' }}" \
            "${{ github.event.inputs.user_password || 'pixel3xl' }}" | \
          pmbootstrap install --no-fde 2>&1

          echo "✅ Installation image created"

      # ── 14. Export images ────────────────────────────────────────────
      - name: Export flashable images
        run: |
          set -euo pipefail
          export PATH="$HOME/.local/bin:$PATH"

          EXPORT_DIR="/tmp/pmos-images"
          mkdir -p "$EXPORT_DIR"

          pmbootstrap export "$EXPORT_DIR" 2>&1

          echo "📂 Exported files:"
          ls -lah "$EXPORT_DIR/"

          IMG_COUNT=$(find "$EXPORT_DIR" -name "*.img" -size +1M | wc -l)
          if [ "$IMG_COUNT" -eq 0 ]; then
            echo "❌ ERROR: No .img files found!"
            exit 1
          fi
          echo "✅ Found $IMG_COUNT image file(s)"

          cat > "$EXPORT_DIR/FLASH_INSTRUCTIONS.txt" << 'EOF'
          =====================================================
           FLASH INSTRUCTIONS — postmarketOS on Pixel 3 XL
          =====================================================

          1. Boot into fastboot:
             Power off → hold Volume Down + Power

          2. Verify: fastboot devices

          3. Set slot: fastboot set_active a

          4. Flash:
             fastboot flash boot boot.img
             fastboot flash userdata google-crosshatch.img

          5. Reboot: fastboot reboot

          6. SSH: ssh user@172.16.42.1
          =====================================================
          EOF
          sed -i 's/^          //' "$EXPORT_DIR/FLASH_INSTRUCTIONS.txt"

      # ── 15. Upload artifacts ─────────────────────────────────────────
      - name: Upload flashable images
        uses: actions/upload-artifact@v4
        with:
          name: postmarketOS-crosshatch-${{ github.event.inputs.ui || 'plasma-mobile' }}
          path: /tmp/pmos-images/
          retention-days: 30
          if-no-files-found: error

      # ── 16. Summary ──────────────────────────────────────────────────
      - name: Print summary
        if: always()
        run: |
          echo ""
          echo "╔══════════════════════════════════════════════════╗"
          echo "║  BUILD COMPLETE — Pixel 3 XL (crosshatch)       ║"
          echo "║  UI: ${{ github.event.inputs.ui || 'plasma-mobile' }}"
          echo "║  Download the artifact from the Actions tab      ║"
          echo "╚══════════════════════════════════════════════════╝"
